# buf generate
# Configuration for the buf generate command
# Uses remote plugins, no separate install required.
version: v2
inputs:
  - directory: .
plugins:
  - remote: buf.build/community/stephenh-ts-proto:v2.6.1
    opt: outputEncodeMethods=false,outputPartialMethods=false,outputServices=false,oneof=unions-value,importSuffix=.js,env=node
    out: .